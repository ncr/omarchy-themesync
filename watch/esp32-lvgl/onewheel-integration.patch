# Integration of Theme Protocol v1 into the onewheel watch firmware (ESP-IDF 5.5.1, LVGL 9.5, NimBLE).
# Generated 2026-08-25T23:21:13+02:00 against ~/dev/onewheel/watch as of that moment; built OK in a scratchpad copy (app 0x1281c0 B).
# Apply from ~/dev/onewheel/watch:  patch -p1 < .../onewheel-integration.patch  (after copying the three modules, see INTEGRATION.md)
--- a/main/CMakeLists.txt
+++ b/main/CMakeLists.txt
@@ -1,5 +1,5 @@
 idf_component_register(
-    SRCS "main.c" "axp2101_port.cpp" "pcf85063.c" "audio.c" "ble.c" "theme.c"
+    SRCS "main.c" "axp2101_port.cpp" "pcf85063.c" "audio.c" "ble.c" "theme.c" "theme_proto.c" "theme_gatt.c"
          "fonts/ow_mont_14.c" "fonts/ow_mont_16.c" "fonts/ow_mont_20.c" "fonts/ow_mont_28.c"
     INCLUDE_DIRS "."
     REQUIRES XPowersLib esp_driver_i2c esp_timer nvs_flash esp_hw_support espressif__esp_codec_dev espressif__esp_lvgl_port espressif__esp_lcd_touch esp_driver_spi esp_lcd esp_driver_gpio espressif__button bt fatfs
--- a/main/ble.c
+++ b/main/ble.c
@@ -6,9 +6,7 @@
 #include "nimble/nimble_port_freertos.h"
 #include "host/ble_hs.h"
 #include "host/util/util.h"
-#include "services/gap/ble_svc_gap.h"
-#include "services/gatt/ble_svc_gatt.h"
-#include "theme.h"
+#include "theme_gatt.h"
 
 static const char *TAG = "ble";
 
@@ -21,15 +19,7 @@
 #define SCAN_MS    5000     // one-shot scan length
 #define DEVICE_NAME "OW-Watch"
 
-// Theme service 7a0e0001-0f0e-4d0c-9c0b-0a0908070605 with two characteristics:
-//   ...0002 "colors": 13 bytes (theme.h wire format), read + write
-//   ...0003 "name":   UTF-8 theme name, <= 31 bytes, read + write
-// (BLE_UUID128_INIT takes the bytes little-endian, i.e. the UUID string reversed.)
-#define THEME_UUID(last) BLE_UUID128_INIT(0x05, 0x06, 0x07, 0x08, 0x09, 0x0a, 0x0b, 0x9c, \
-                                          0x0c, 0x4d, 0x0e, 0x0f, last, 0x00, 0x0e, 0x7a)
-static const ble_uuid128_t THEME_SVC_UUID    = THEME_UUID(0x01);
-static const ble_uuid128_t THEME_COLORS_UUID = THEME_UUID(0x02);
-static const ble_uuid128_t THEME_NAME_UUID   = THEME_UUID(0x03);
+// The Theme Protocol v1 GATT service (7e450001-…) lives in theme_gatt.c.
 
 typedef struct {
     uint8_t addr[6];
@@ -113,88 +103,16 @@
 }
 
 // ---- theme service ---------------------------------------------------------------------
-static int theme_access(uint16_t conn_handle, uint16_t attr_handle,
-                        struct ble_gatt_access_ctxt *ctxt, void *arg)
-{
-    (void)conn_handle; (void)attr_handle; (void)arg;
-    const bool is_colors = ble_uuid_cmp(ctxt->chr->uuid, &THEME_COLORS_UUID.u) == 0;
-    switch (ctxt->op) {
-    case BLE_GATT_ACCESS_OP_READ_CHR: {
-        face_theme_t t;
-        theme_copy(&t);
-        int rc;
-        if (is_colors) {
-            uint8_t w[THEME_WIRE_LEN];
-            theme_to_wire(&t, w);
-            rc = os_mbuf_append(ctxt->om, w, sizeof(w));
-        } else {
-            rc = os_mbuf_append(ctxt->om, t.name, strlen(t.name));
-        }
-        return rc == 0 ? 0 : BLE_ATT_ERR_INSUFFICIENT_RES;
-    }
-    case BLE_GATT_ACCESS_OP_WRITE_CHR: {
-        uint8_t buf[THEME_NAME_MAX + 1];
-        uint16_t len = 0;
-        if (ble_hs_mbuf_to_flat(ctxt->om, buf, sizeof(buf) - 1, &len) != 0) return BLE_ATT_ERR_UNLIKELY;
-        if (is_colors) return theme_set_wire(buf, len) ? 0 : BLE_ATT_ERR_INVALID_ATTR_VALUE_LEN;
-        if (len > THEME_NAME_MAX - 1) len = THEME_NAME_MAX - 1;
-        buf[len] = '\0';
-        theme_set_name((const char *)buf);
-        return 0;
-    }
-    default:
-        return BLE_ATT_ERR_UNLIKELY;
-    }
-}
-
-static const struct ble_gatt_svc_def s_gatt_svcs[] = {
-    {
-        .type = BLE_GATT_SVC_TYPE_PRIMARY,
-        .uuid = &THEME_SVC_UUID.u,
-        .characteristics = (struct ble_gatt_chr_def[]) {
-            { .uuid = &THEME_COLORS_UUID.u, .access_cb = theme_access,
-              .flags = BLE_GATT_CHR_F_READ | BLE_GATT_CHR_F_WRITE },
-            { .uuid = &THEME_NAME_UUID.u, .access_cb = theme_access,
-              .flags = BLE_GATT_CHR_F_READ | BLE_GATT_CHR_F_WRITE },
-            { 0 }
-        },
-    },
-    { 0 }
-};
-
-// Advertise slowly (300-500 ms): enough to be found in a couple of seconds, and light
-// enough that the radio does not get in the way of the display DMA the way the scan did.
+// Advertising, connections and the Theme Protocol v1 characteristics are owned by
+// theme_gatt.c (it re-advertises after a disconnect itself); this is just the switch.
 static void start_advertising(void)
 {
-    if (s_advertising || s_connected) return;
-    struct ble_hs_adv_fields f = {0};
-    f.flags = BLE_HS_ADV_F_DISC_GEN | BLE_HS_ADV_F_BREDR_UNSUP;
-    f.tx_pwr_lvl_is_present = 1;
-    f.tx_pwr_lvl = BLE_HS_ADV_TX_PWR_LVL_AUTO;
-    const char *name = ble_svc_gap_device_name();
-    f.name = (const uint8_t *)name;
-    f.name_len = (uint8_t)strlen(name);
-    f.name_is_complete = 1;
-    int rc = ble_gap_adv_set_fields(&f);
-    if (rc != 0) { ESP_LOGW(TAG, "adv_set_fields rc=%d", rc); return; }
-    struct ble_hs_adv_fields rsp = {0};
-    rsp.uuids128 = (ble_uuid128_t *)&THEME_SVC_UUID;
-    rsp.num_uuids128 = 1;
-    rsp.uuids128_is_complete = 1;
-    rc = ble_gap_adv_rsp_set_fields(&rsp);
-    if (rc != 0) ESP_LOGW(TAG, "adv_rsp_set_fields rc=%d", rc);
-    struct ble_gap_adv_params ap = {0};
-    ap.conn_mode = BLE_GAP_CONN_MODE_UND;
-    ap.disc_mode = BLE_GAP_DISC_MODE_GEN;
-    ap.itvl_min = BLE_GAP_ADV_ITVL_MS(300);
-    ap.itvl_max = BLE_GAP_ADV_ITVL_MS(500);
-    rc = ble_gap_adv_start(s_own_addr_type, NULL, BLE_HS_FOREVER, &ap, gap_event, NULL);
-    if (rc == 0) { s_advertising = true; ESP_LOGI(TAG, "advertising as %s with the theme service", name); }
-    else ESP_LOGW(TAG, "adv_start rc=%d", rc);
+    if (s_advertising || theme_gatt_connected()) return;
+    if (theme_gatt_advertise(s_own_addr_type) == 0) s_advertising = true;
 }
 
 void ble_start_advertising(void) { start_advertising(); }
-bool ble_link_connected(void)    { return s_connected; }
+bool ble_link_connected(void)    { return theme_gatt_connected(); }
 
 static void start_scan(void)
 {
@@ -222,14 +140,9 @@
     if (nimble_port_init() != ESP_OK) { ESP_LOGE(TAG, "nimble_port_init failed"); return false; }
     ble_hs_cfg.sync_cb  = on_sync;
     ble_hs_cfg.reset_cb = on_reset;
-    // GATT: the standard GAP/GATT services plus our theme service, registered before the
-    // host starts (services cannot be added once it is running).
-    ble_svc_gap_init();
-    ble_svc_gatt_init();
-    int rc = ble_gatts_count_cfg(s_gatt_svcs);
-    if (rc == 0) rc = ble_gatts_add_svcs(s_gatt_svcs);
-    if (rc != 0) ESP_LOGE(TAG, "gatt service registration rc=%d", rc);
-    ble_svc_gap_device_name_set(DEVICE_NAME);
+    // GATT: GAP/GATT services plus the Theme Protocol v1 service (theme_gatt.c), registered
+    // before the host starts (services cannot be added once it is running).
+    theme_gatt_register(DEVICE_NAME);
     nimble_port_freertos_init(host_task);
     return true;
 }
--- a/main/main.c
+++ b/main/main.c
@@ -33,6 +33,7 @@
 #include "audio.h"
 #include "ble.h"
 #include "theme.h"
+#include "theme_gatt.h"
 #include <string.h>
 #include <stdarg.h>
 #include "driver/spi_master.h"
@@ -359,37 +360,46 @@
 #define SCREEN_CORNER_RADIUS_PX  115
 
 
-// ---- theme -> shared styles --------------------------------------------------------------
-// Every coloured object on the screen takes its colour from one of these styles, never from
-// a literal, so a new theme (pushed over BLE, or loaded from NVS at boot) is one
-// styles_apply_theme() away: lv_obj_report_style_change(NULL) restyles everything in place.
-static lv_style_t st_screen, st_card, st_text, st_dim, st_accent_text,
-                  st_bar_track, st_bar_accent, st_bar_warn,
-                  st_btn_accent, st_btn_warn, st_btn_text;
+// ---- theme -> shared styles (ThemeManager, theme.h) --------------------------------------
+// Every coloured object on the screen takes its colour from a shared style owned by theme.c,
+// never from a literal. A Theme Protocol packet from the desktop (theme_gatt.c) or the NVS
+// copy at boot goes through theme_apply(), which rewrites those styles and calls
+// lv_obj_report_style_change(NULL): everything restyles in place. The st_* names are
+// aliases so the widget code below reads the same as before.
+#define st_screen        (*theme_style(THEME_STYLE_SCREEN))
+#define st_card          (*theme_style(THEME_STYLE_CARD))
+#define st_text          (*theme_style(THEME_STYLE_TEXT))
+#define st_dim           (*theme_style(THEME_STYLE_TEXT_SECONDARY))
+#define st_accent_text   (*theme_style(THEME_STYLE_TEXT_ACCENT))
+#define st_bar_track     (*theme_style(THEME_STYLE_WELL))
+#define st_bar_accent    (*theme_style(THEME_STYLE_INDICATOR_ACCENT))
+#define st_bar_warn      (*theme_style(THEME_STYLE_INDICATOR_WARNING))
+#define st_btn_accent    (*theme_style(THEME_STYLE_BUTTON_ACCENT))
+#define st_btn_warn      (*theme_style(THEME_STYLE_BUTTON_WARNING))
+#define st_btn_text      (*theme_style(THEME_STYLE_TEXT_ON_ACCENT))
+#define st_btn_text_warn (*theme_style(THEME_STYLE_TEXT_ON_WARNING))
 
-static void styles_init(void)
+// Bidirectional sync: the three buttons on the THEME card notify the desktop (Control
+// characteristic); themesync runs `omarchy-theme-set` there and the new theme comes back
+// through the normal push path.
+static void theme_button_cb(lv_event_t *e)
 {
-    lv_style_t *all[] = { &st_screen, &st_card, &st_text, &st_dim, &st_accent_text, &st_bar_track,
-                          &st_bar_accent, &st_bar_warn, &st_btn_accent, &st_btn_warn, &st_btn_text };
-    for (size_t i = 0; i < sizeof(all) / sizeof(all[0]); i++) lv_style_init(all[i]);
-    lv_style_set_bg_opa(&st_screen, LV_OPA_COVER);
+    uint8_t op = (uint8_t)(intptr_t)lv_event_get_user_data(e);
+    int rc = theme_gatt_send_control(op);
+    ESP_LOGI(TAG, "theme control %u -> rc=%d", op, rc);
+    if (rc != 0 && s_audio_ok) audio_beep();   // nobody connected: audible "no"
 }
 
-static void styles_apply_theme(const face_theme_t *t)
+static lv_obj_t *make_theme_button(lv_obj_t *parent, const char *txt, uint8_t op)
 {
-    const lv_color_t bg = t->force_black ? lv_color_black() : t->bg;
-    lv_style_set_bg_color(&st_screen, bg);
-    lv_style_set_bg_color(&st_card, t->card);
-    lv_style_set_text_color(&st_text, t->fg);
-    lv_style_set_text_color(&st_dim, t->muted);
-    lv_style_set_text_color(&st_accent_text, t->accent);
-    lv_style_set_bg_color(&st_bar_track, t->dim);
-    lv_style_set_bg_color(&st_bar_accent, t->accent);
-    lv_style_set_bg_color(&st_bar_warn, t->warn);
-    lv_style_set_bg_color(&st_btn_accent, t->accent);
-    lv_style_set_bg_color(&st_btn_warn, t->warn);
-    lv_style_set_text_color(&st_btn_text, bg);
-    lv_obj_report_style_change(NULL);   // NULL = every style changed -> refresh all users
+    lv_obj_t *b = lv_button_create(parent);
+    lv_obj_add_style(b, &st_btn_accent, 0);
+    lv_obj_add_event_cb(b, theme_button_cb, LV_EVENT_CLICKED, (void *)(intptr_t)op);
+    lv_obj_t *l = lv_label_create(b);
+    lv_label_set_text(l, txt);
+    lv_obj_add_style(l, &st_btn_text, 0);
+    lv_obj_set_style_text_font(l, &ow_mont_20, 0);
+    return b;
 }
 
 static lv_obj_t *make_card(lv_obj_t *parent)
@@ -487,15 +497,10 @@
     (void)t;
     s_ui_beat++;
 
-    // ---- theme (pushed over BLE, or loaded from NVS at boot) -> restyle in place ----
-    if (theme_take_dirty()) {
-        face_theme_t th;
-        theme_copy(&th);
-        styles_apply_theme(&th);
-        label_printf(lbl_theme, "theme  %s   bg #%06x  fg #%06x  accent #%06x", th.name,
-                     (unsigned)(lv_color_to_u32(th.bg) & 0xffffff), (unsigned)(lv_color_to_u32(th.fg) & 0xffffff),
-                     (unsigned)(lv_color_to_u32(th.accent) & 0xffffff));
-    }
+    // ---- theme readout (the restyle itself happens in theme_apply(), under the LVGL lock,
+    // when theme_gatt.c receives a packet or theme_init() restores one from NVS) ----
+    label_printf(lbl_theme, "theme  %s   %s   %u colours   crc %04x",
+                 theme.name, theme.light ? "light" : "dark", theme.n_applied, theme.crc);
 
     // Only formats values sensor_task already read: nothing here touches I2C or the log,
     // so nothing can stall the LVGL task between two frames of a scroll.
@@ -563,8 +568,8 @@
                                   ble_device_count(),
                                   ble_onewheel_seen() ? "  (Onewheel!)" : "", list);
         }
-        label_printf(lbl_ble_link, "%s", ble_link_connected() ? "theme service: desktop connected"
-                                        : ble_scan_done()     ? "advertising as OW-Watch  |  theme service"
+        label_printf(lbl_ble_link, "%s", ble_link_connected() ? "Theme Protocol v1: desktop connected  |  PREV / MODE / NEXT switch the desktop theme"
+                                        : ble_scan_done()     ? "advertising as OW-Watch  |  run `themesync sync` on the desktop"
                                                               : "scan first, then advertise");
     }
 
@@ -647,10 +652,7 @@
     lv_obj_set_size(frame, w, h);          // exact panel size, not a percentage
     lv_obj_set_pos(frame, 0, 0);           // top-left visible pixel
     lv_obj_set_style_radius(frame, SCREEN_CORNER_RADIUS_PX, 0);
-    lv_obj_set_style_bg_opa(frame, LV_OPA_TRANSP, 0);
-    lv_obj_set_style_border_color(frame, lv_color_white(), 0);
-    lv_obj_set_style_border_opa(frame, LV_OPA_COVER, 0);
-    lv_obj_set_style_border_width(frame, 1, 0);   // 1px on the outermost visible pixels
+    lv_obj_add_style(frame, theme_style(THEME_STYLE_FRAME), 0);   // 1px accent ring on the outermost visible pixels
     lv_obj_set_style_pad_all(frame, 0, 0);
     lv_obj_clear_flag(frame, LV_OBJ_FLAG_CLICKABLE);
     lv_obj_clear_flag(frame, LV_OBJ_FLAG_SCROLLABLE);
@@ -817,10 +819,24 @@
     lv_obj_add_style(h2, &st_dim, 0);
 
     // ---- status line ----
-    lv_obj_t *st_card = make_card(scr);
-    lbl_status = make_value(st_card, &ow_mont_16);
+    lv_obj_t *status_card = make_card(scr);
+    lbl_status = make_value(status_card, &ow_mont_16);
     lv_obj_add_style(lbl_status, &st_accent_text, 0);
 
+    // ---- theme card: Omarchy sync, bidirectional (theme.h / theme_gatt.h) ----
+    lv_obj_t *th_card = make_card(scr);
+    make_title(th_card, "THEME  |  Omarchy sync  |  Theme Protocol v1");
+    lv_obj_t *th_row = lv_obj_create(th_card);
+    lv_obj_remove_style_all(th_row);
+    lv_obj_set_width(th_row, lv_pct(100));
+    lv_obj_set_height(th_row, LV_SIZE_CONTENT);
+    lv_obj_set_flex_flow(th_row, LV_FLEX_FLOW_ROW);
+    lv_obj_set_flex_align(th_row, LV_FLEX_ALIGN_SPACE_BETWEEN, LV_FLEX_ALIGN_CENTER, LV_FLEX_ALIGN_CENTER);
+    lv_obj_clear_flag(th_row, LV_OBJ_FLAG_SCROLLABLE);
+    make_theme_button(th_row, "PREV", THEME_CTRL_PREV);
+    make_theme_button(th_row, "MODE", THEME_CTRL_TOGGLE_MODE);
+    make_theme_button(th_row, "NEXT", THEME_CTRL_NEXT);
+
     // ---- IMU card ----
     lv_obj_t *imu_card = make_card(scr);
     make_title(imu_card, "IMU  |  QMI8658  |  accelerometer / gyro");
@@ -881,7 +897,7 @@
     lv_obj_add_event_cb(buzz, buzz_event_cb, LV_EVENT_CLICKED, NULL);
     lv_obj_t *buzz_txt = lv_label_create(buzz);
     lv_label_set_text(buzz_txt, "BUZZ");
-    lv_obj_add_style(buzz_txt, &st_btn_text, 0);
+    lv_obj_add_style(buzz_txt, &st_btn_text_warn, 0);
     lv_obj_set_style_text_font(buzz_txt, &ow_mont_20, 0);
 
     bar_mic = lv_bar_create(au_row);
@@ -1121,7 +1137,6 @@
         ret = nvs_flash_init();
     }
     ESP_ERROR_CHECK(ret);
-    theme_init();   // colour roles: built-in "mono", overridden by whatever the desktop last pushed (NVS)
 
     // Bring up AMOLED + touch + LVGL. Pin the LVGL port task to core 0 so it never shares
     // a core with BLE (pinned to core 1) — an unpinned LVGL task drifting onto the BLE core
@@ -1237,8 +1252,9 @@
     (void)chip; (void)flash_sz;
     build_calibration_screen();
 #else
-    styles_init();
-    { face_theme_t th; theme_copy(&th); styles_apply_theme(&th); }
+    // Palette first (built-in, then the last theme the desktop pushed, from NVS), so the UI
+    // is built with the right colours. Needs LVGL up and the lock held: it touches styles.
+    theme_init();
     build_ui();
     build_screen_frame();
 
